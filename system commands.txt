git remote -v -- to check current repo
git remote set-url origin <new-repo-url> -- to set new repo


git remote add origin git@github.com:Meena044/git_test.git -- to link the repo
git remote add origin git@github.com:Meena044/git_test.git -- to set the repo in vscode

sudo apt update
sudo apt install code

 xinput list
xinput disable 11 -mouse touchpad disable
xinput enable 11 -mouse touchpad enable

redshift -O 4500   # Warm white (recommended night setting)
redshift -O 3500   # Amber light (deeper night mode)
redshift -O 3000   # Red-orange (strongest eye comfort)
redshift -O 6500   # Reset to normal daylight temperature
